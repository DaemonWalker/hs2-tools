# 03 阶段计划

实施顺序与验收标准。总估算 **24~33 人天**（1 人全栈熟练工口径）。
前置阅读：[README.md](README.md)（决策）、[01-现状分析.md](01-现状分析.md)、[02-迁移方案.md](02-迁移方案.md)。

> 进度（2026-07-23）：全部阶段（0~4）已完成，验收结果见各节"完成记录"；阶段 1 实施期修订见 [02-迁移方案.md](02-迁移方案.md) 第 10 节。

## 阶段总览

| 阶段 | 内容 | 验收标准 | 估算 | 状态 |
|---|---|---|---|---|
| 0. 准备 | 建 sln；收集测试夹具 | 测试数据就绪 | 0.5 天 | ✅ |
| 1. 核心服务移植 | 四个服务 + xUnit 单测 | 与 Go 版输出对照一致 | **5~8 天** | ✅ |
| 2. 应用骨架 | 服务容器、WindowManager、主窗口导航、单主题资源字典 | 空窗口可导航、配置可读写 | 2 天 | ✅ |
| 3. 页面迁移 | 8 个窗口 + CardGridControl | 每页功能对照可用 | 15~18 天 | ✅ |
| 4. 打磨打包 | 细节、异常提示、发布 | 单文件自包含 exe 可用 | 2~3 天 | ✅ |

## 阶段 0：准备（0.5 天）

- 创建 `HS2Tools.sln`：`src/HS2Tools`（net8.0-windows，UseWPF）+ `tests/HS2Tools.Tests`（xUnit）。
- 收集测试夹具（**不提交大文件进仓库**，用本地路径或 gitignore 的 fixtures 目录）：
  - 真实 HS2 角色卡 PNG 若干（含中文名、含乱码名、无 Mod、多 Mod 的样本）；
  - 真实 zipmod 若干（含 Store/Deflate 两种压缩、含脏 manifest 的样本）；
  - sideload.zip 与一份爬虫目标页面的 HTML 抓样（离线解析用）。

**完成记录**：`hs2-tools-csharp/HS2Tools.sln` 已建（`src/HS2Tools` WPF + `tests/HS2Tools.Tests` xUnit，测试项目 TFM 为 net8.0-windows 以引用 WPF 项目；`InternalsVisibleTo` 打通内部类型测试）。sideload.zip 已作 `EmbeddedResource` 内嵌。夹具改为**程序化合成**（见 02 §10.3）：合成 PNG 标记字节、Store/Deflate/脏 manifest zipmod、1500 条目大 zipmod（中央目录 ~186KB，强制渐进抓取）、autoindex HTML、HttpListener 内存测试服务器（Range/HEAD/慢速流式）。

## 阶段 1：核心服务移植（5~8 天，**成败关键，优先做**）

顺序：utils → scanner → downloader → sideloader（风险递增）。

1. `ConfigService` + `Models`：强类型配置读写（含"文件不存在返回空配置而非异常"的原版语义）。
2. `ScannerService`：目录扫描（子串排除、扩展名不敏感）、PNG 字节搜索解析、zipmod manifest 解析（`cleanString` 按 rune 过滤）、批量并行（`Parallel.ForEachAsync`）。
3. `DownloaderService` + `DownloadManager`：断点续传（206/200 分支）、代理（含认证）、进度节流、取消。
4. `SideloaderService`：HTML 目录解析（HtmlAgilityPack）、远程 ZIP 中央目录解析（渐进式尾部抓取 + 裸 DEFLATE）、两级信号量并发、可重入 Run。
5. `GameLauncherService`：`Process.Start` 启动游戏/工作室、`explorer /select,`、`SetThreadExecutionState` 防休眠。

**验证策略（必须执行）**：

- Go 版 CLI（`wails/cmd/scanner`、`wails/cmd/sideloader`）作为**基准工具**：对同一批夹具分别跑 Go 版与 C# 版，GUID/角色名提取结果逐一比对，要求完全一致。
- 单测覆盖：PNG 标记搜索（去首尾字节、IEND 截断）、zipmod cleanString（控制字符、非 ASCII、代理对）、EOCD 边界（comment、ZIP64 放弃）、DEFLATE 解压、Content-Range 解析。
- 爬虫全量跑通一次（真实站点 + 代理），结果 guid 数量与 Go 版相当。

**完成记录**：

- 5 个服务全部移植完成（`src/HS2Tools/Services/`），xUnit **104 项全绿**（含字节级边界用例与本地服务器集成测试）。
- Go scanner CLI 对照：7 项对照测试（readPngNames/Mods/Image、readZipMod、scanDir 含遍历顺序、两个批量）全过；测试自动在临时副本构建基准 CLI（不改原仓库；go.mod `go 1.25.0` 与本机 go1.24 的兼容处理见 02 §10.3）。
- Go sideloader CLI 真实爬取基准 12,472 个；C# 对照爬取 12,366 个（29 分钟），**guid 重合度 99.06%**，相对路径差异 75 个（重复 guid 的多目录选路，非提取错误，见 02 §10.3）。
- 实施期发现并修复 .NET 平台陷阱 3 个（系统代理、h2 无效设置、200 全量）+ 安全防御 2 项，详见 [02-迁移方案.md](02-迁移方案.md) 第 10 节——**阶段 3 涉及网络下载前必读**。

## 阶段 2：应用骨架（2 天）

- `App.xaml.cs`：服务容器初始化、异常兜底（`DispatcherUnhandledException` 记日志+提示）、主窗口启动。
- `WindowManager`：窗口单例 + Show/Hide/Activate + 主窗关闭退出。
- 单主题 `ResourceDictionary`（颜色/画刷集中定义，控件一律 `DynamicResource`——为 A7 后期双主题留口）。
- `MainWindow`：导航入口 + 首页静态布局占位。
- `ConfigService` 接入：游戏路径设置 + `HoneySelect2.exe` 校验徽标。

**完成记录**：全部落实。`Services/ServiceContainer.cs`（单例容器，代理经 `Config.GetProxyString` 即时生效）；`Windows/WindowManager.cs`（子窗口 Closing 拦截为 Hide 保状态，主窗关闭即退出）；`Themes/Theme.xaml`（颜色/画刷集中定义）；`Views/` MainWindow（游戏路径卡片 + 红绿校验徽标 + 7 导航按钮）+ 7 占位窗口；`ViewModels/MainWindowViewModel.cs`（CommunityToolkit.Mvvm）。新增 7 项测试（VM 校验/持久化 + WindowManager STA 冒烟含 8 窗口 XAML 实例化），`dotnet run` 启动冒烟通过。

## 阶段 3：页面迁移（15~18 天）

按依赖顺序推进，每页完成后即可独立验收：

| 序 | 页面 | 要点 | 估算 |
|---|---|---|---|
| 1 | MainWindow(Home) | 扫描流程（三阶段+进度）、启动游戏/工作室（A1）、sideloader 更新（运行/停止/日志）、缺失补全（批量下载+进度弹窗）、统计 | 3 天 |
| 2 | CardGridControl | 虚拟化网格（自实现 VirtualizingWrapPanel）、缩略图 LRU 缓存、收藏、排序（zh-CN） | 2~3 天 |
| 3 | CharaWindow | 网格 + 搜索（A2）+ 详情面板（A4 一键下载、A6 真实文件信息） | 2 天 |
| 4 | SceneWindow | 场景库 + 智能整理（任务模型、进度、批量执行） | 3 天 |
| 5 | ModsWindow | DataGrid + 统计 + 筛选（所有/未使用）+ 刷新 | 1 天 |
| 6 | SideloadWindow | DataGrid + 防抖搜索 + 已存在/缺失 + 下载 | 1.5 天 |
| 7 | DownloadWindow | 统计栏 + Tab + 任务项（进度/速度/剩余时间/取消/重试） | 1.5 天 |
| 8 | CardExplorerWindow | 文件选择 + 人物/场景两种布局 | 1 天 |
| 9 | SettingsWindow | 代理表单 + 防休眠开关（SetThreadExecutionState） | 0.5 天 |

**完成记录**（2026-07-22）：9 个页面全部完成，xUnit **194 项全绿**（+1 真实站点对照跳过），`dotnet run` 启动冒烟通过。

- **新增服务/控件**：`SideloadDatabaseService`（爬虫结果落盘 `%AppData%/hs2-tools/sideload.json`，用户库覆盖内嵌库——**修复原版更新从不生效**：complete 事件数据被前端丢弃、init() 重读内嵌旧库）；`ISideloaderService` 抽象（VM 可测）；`Controls/`：CardGridControl（虚拟化网格 + 分批 24 加载 + 收藏/排序/搜索）、VirtualizingWrapPanel（固定项尺寸 + IScrollInfo，约 200 行）、ThumbnailCache（LRU 500 张）、CardDetailPanel（角色/场景共用详情：A4 一键下载、A6 真实文件信息）、CardSortHelper（zh-CN 排序，与浏览器 ICU 行为一致）、UiDispatch、FormatUtils。
- **各页要点**：MainWindow 三阶段扫描（BATCH_SIZE 500、usage 合并照原版 `{...scene,...female}` 角色覆盖场景）+ 快速启动（A1）+ 爬虫运行/停止确认 + 缺失补全（串行等待 TaskFinished，失败不阻断）；CharaWindow 搜索接通（A2）；SceneWindow 智能整理（任务模型、0-50 分析/50-100 移动进度）；ModsWindow 使用次数大小写不敏感匹配；SideloadWindow 300ms 防抖（版本号作废旧查询）；DownloadWindow 四 Tab（**失败仅 Failed 不含 Cancelled**，照原版）；CardExplorer 人物/场景双布局；Settings 代理校验（http/https/socks5——**有意超集于原版的 http/https**，socks5 本就被 GetProxyString 支持）+ 防休眠开关（App 启动时恢复）。
- **修复的原版 bug（阶段 3 发现）**：① sideloader 更新结果从不持久化（见上）；② 智能整理 `moveFile(scenePath, targetFolder)` 把目录当目标路径，os.Rename(文件→已存在目录) 恒失败、错误只进 console——**整理从未真正移动文件**；C# 版目标补全文件名 `hs_tools_<文件夹>/<场景文件名>`。详见 [02-迁移方案.md](02-迁移方案.md) §10.4。

## 阶段 4：打磨打包（2~3 天）

**阶段 3 移交的待验证/待办清单（新会话从这里接手）：**

1. **乱码卡名真实卡片终验**（阶段 1 起挂起）：合成夹具已与 Go CLI 对照一致，但 Go `string(bytes)` 保留无效 UTF-8 原始字节、.NET 替换为 U+FFFD 的差异**只验证过合法 UTF-8**。需用真实乱码名卡片对比两版输出，确认差异可接受（02 §9）。
2. **真实环境冒烟**（全程合成夹具，未碰过真实游戏）：按下发冒烟清单在真实 HS2 安装上跑一遍；真实 sideload.zip（12k 条目）下 SideloadWindow 滚动/搜索性能确认；真实 mods 目录（数千 zipmod）扫描耗时确认。
3. **全局异常提示与操作反馈统一**：`App.DispatcherUnhandledException` 兜底已有（记 error.log + MessageBox）；检查各页失败路径（扫描中断、下载失败、整理失败、爬虫失败）的提示文案是否一致、是否都有日志。
4. **长任务防重入盘点**：扫描/补全/爬虫/整理/刷新已有 CanExecute 或标志位防重入；逐一确认（尤其整理执行中删除/清空任务的禁用、窗口 Hide/Show 期间任务状态展示）。
5. **空状态/无图占位盘点**：各网格/表格/详情/单卡查看的空态文案与"暂无预览"占位。
6. **发布**：`dotnet publish -c Release -r win-x64 --self-contained -p:PublishSingleFile=true`。验证点：内嵌 sideload.zip 随单文件正常读取（`SideloadDatabaseService` 回退路径）；首次启动在干净 `%AppData%` 下建配置；单文件 exe 全流程冒烟。
7. **冒烟清单**（DoD）：首次启动 → 设路径 → 扫描 → 浏览卡片 → 下载（含代理）→ 整理场景 → 爬虫更新 → 启动游戏；并确认 `settings.json` 单一配置生效、无 Wails/React/Go 运行时依赖。

**已知与原版的有意差异（阶段 4 若接到"不一致"反馈先查此表，02 §10.4 有全表）**：sideloader 更新真实落盘、智能整理真实移动、代理校验放行 socks5、下载页"清除完成"清所有非下载中任务（DownloadManager.ClearFinished 语义，见 02 §10.4）。

**完成记录**（2026-07-23）：xUnit **202 项全绿**（总计 206，4 项默认跳过：真实站点对照 + 3 项 env 门控真实环境基准）；发布产物 `publish/HS2Tools.exe` 单文件自包含 162MB，启动冒烟通过。清单逐项：

1. **乱码卡名终验 ✅**：真实安装 `F:\game\HS2V24` 递归全量 **15,141 张卡**与 Go CLI 逐一比对名称 + Mod GUID，**输出完全一致**（约 5 分钟）；其中 **5 张含 U+FFFD 乱码名**——乱码路径确认被覆盖；Go 侧 `encoding/json` 输出边界同样做 U+FFFD 替换，两版在应用边界无差异，02 §9 担忧消解。对照用例 `GoComparisonTests.RealCards_NamesAndMods_MatchGo`（env `HS2_REAL_CARD_DIRS` 门控）留存可复跑。
2. **真实环境基准 ✅**：真实 mods 目录递归 **6,325 个 zipmod** 全量扫描 **29 秒**（→ 6,136 个 guid，`ScannerTests.RealModsDir_ScanBenchmark`）；真实游戏目录三阶段扫描 **48.4 秒**（mods=6,136、usage=3,974、refs=76,624、missing=172，`MainWindowViewModelTests.Scan_RealGameDir_Benchmark`）；真实内嵌库 **11,958 条目**建行 **9ms**、防抖过滤 **1ms**（`SideloadWindowViewModelTests.RealBundledDatabase_ReloadAndSearch_Performance`，默认运行）。交互式 UI 冒烟（设路径→扫描→浏览→下载→整理→爬虫→启动游戏）移交用户在发布 exe 上执行。
3. **异常提示与日志统一 ✅**：新建 `Services/ErrorLog.cs` 统一设施（%AppData%/hs2-tools/error.log，永不抛出），`App.LogException` 委托之。原版"静默吞错"点全部留痕：ConfigService 写盘失败/损坏回退、下载失败、爬虫单页失败与单 mod 失败链、SideloadDatabase 损坏回退、ScanDirectory 跳过点、非法代理回退、单卡解析失败、详情加载失败。文案统一"XX失败：原因"（整理失败原为裸 ex.Message）。
4. **防重入盘点 ✅**：扫描/补全/爬虫/整理/刷新防重入机制全部存在且正确（CanExecute + 标志位双保险、防抖版本号、单卡查看版本号均验证无误）；窗口 Hide/Show 期间任务状态经 VM 单例 + 事件封送无损恢复。修复 2 个真实竞态：CardGridControl reset 撞上在飞批次停空态（finally 接力补触发）、下载窗口终态同名任务重下同 Id 双行（TaskAdded 按 Id 去重）。整理执行中删除/清空从"点击静默无效"改为按钮置灰（CanExecute）。扫描∥补全∥爬虫∥Mods 刷新允许并行（与原版一致，见 02 §10.5）。搜索框"未实时过滤"经核实为**审计误报**（ElementName 绑 TextBox.Text 作源即逐键传播），未改。
5. **空状态/占位 ✅**：修复 CardGridControl 缩略图 Image 可见性触发器写反的**严重 bug**（缩略图从不显示、"暂无预览"常驻——阶段 3 冒烟未渲染缩略图故未发现）；补 CardDetailPanel 大图"暂无预览"与"未检测到模组依赖"空态；Chara/Scene 网格未设游戏路径时显示引导文案（EmptyText 依赖属性）；"所有 Mods 已就绪"门控 HasScanData（无扫描数据不再误显示）。
6. **发布 ✅**：`dotnet publish -c Release -r win-x64 --self-contained -p:PublishSingleFile=true` 通过；单文件 exe 启动冒烟通过；内嵌 sideload.zip 随单文件正常读取（无用户 sideload.json 时回退内嵌库，启动即加载）；干净首启（无 settings.json）正常建配置目录；无 Wails/React/Go 运行时依赖（纯自包含）。测试期 ErrorLog 经 `TestAssets` 静态构造统一重定向，不再污染真实 error.log。
7. **冒烟清单**：设置保存/扫描/下载/整理/爬虫状态机均有 VM 级测试覆盖（含真实目录基准）；单文件 exe 启动冒烟通过；**交互式全流程冒烟（含启动游戏、代理下载）需在用户机器上人工执行**——启动 `hs2-tools-csharp/publish/HS2Tools.exe` 按上述顺序过一遍即可。

另修复的高危项（清单外发现）：`ConfigService` 防抖写盘失败在线程池 Timer 回调抛出**会直接导致进程崩溃**（设置页却显示"设置已保存"）——改为吞掉记日志并保持脏标记下次重试；`CardDetailViewModel.LoadCoreAsync` 无 try/catch（文件竞态下任务 faulted 无人观察）。

## 风险与对策

| 风险 | 等级 | 对策 |
|---|---|---|
| 远程 ZIP 解析移植出错（zipreader） | **高** | 阶段 1 最先移植并配足单测；与 Go CLI 输出对照；保留渐进抓取/前插/EOCD 末尾校验全部边界行为。**→ 已消解**：EOCD/ZIP64/截断窗口/Store/Deflate 单测全过，真实站点对照 12,366/12,472 重合 99.06% |
| PNG 字节搜索行为偏差（编码、乱码名） | 中 | 真实卡片夹具回归；注意 Go 与 .NET 无效 UTF-8 处理差异，测试期确认可接受。**→ 合成夹具（含 UTF-8 中文名）与 Go CLI 对照一致；乱码名真实卡片终验移交阶段 4（见阶段 4 清单第 1 项）** |
| 爬虫目标站点结构变化 | 中 | 选择器逻辑与原版一致即可，非迁移引入的风险；失败路径记日志便于排障 |
| WPF 无内置 VirtualizingWrapPanel | 低 | 自带实现（约 200 行）或按行分组虚拟化 |
| 双主题后期补做成本 | 低 | 阶段 2 起强制 `DynamicResource`，不留硬编码色值。**→ 已落实**（Themes/Theme.xaml 集中定义） |

## 完成定义（DoD）

- 阶段 1：xUnit 全绿；与 Go CLI 对照输出一致；爬虫真实跑通一次。
- 全部阶段：冒烟清单通过；`settings.json` 单一配置生效（启动游戏可用）；无 Wails/React/Go 运行时依赖。
