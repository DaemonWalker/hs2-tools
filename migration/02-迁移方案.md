# 02 迁移方案

本文档定义目标架构与技术方案。最终决策见 [README.md](README.md) 决策记录；
现状依据见 [01-现状分析.md](01-现状分析.md)。

## 1. 总体思路

迁移本质是三件事，工作量差异极大：

| 部分 | 迁移方式 | 工作量 |
|---|---|---|
| Go `internal/` 后端逻辑（~1,460 行） | **逐行移植**为 C# 服务类，逻辑 1:1（含所有字节级 hack） | 小 |
| `app.go` 绑定层 + 前端 IPC 兼容层 | **整个消失**——同进程，ViewModel 直接 `await` 服务，事件用 C# event / `IProgress<T>` | 净删除 |
| React 前端（8,000 行 TSX + 2,000 CSS） | **完全重写**为 WPF（XAML + MVVM） | **大（占总量 70%+）** |

Wails 的批量 API 当初是为减少 IPC 往返设计的；C# 同进程直接循环 + `Parallel.ForEachAsync` 即可，分批壳不再需要（并行度保留：文本 8 / 图像 4 / zipmod 4）。

## 2. 项目结构

```
hs2-tools-csharp/
├── HS2Tools.sln
├── src/HS2Tools/                        # net8.0-windows, UseWPF=true
│   ├── App.xaml / App.xaml.cs           # 入口：初始化服务容器、启动主窗口
│   ├── Services/                        # ← Go internal/ 的直接移植（与 UI 解耦，可单测）
│   │   ├── ScannerService.cs            #   目录扫描、PNG 解析、zipmod 解析
│   │   ├── DownloaderService.cs         #   断点续传下载（对应 downloader 包）
│   │   ├── DownloadManager.cs           #   全局单例：任务表 + 进度/完成事件（对应 app.go 下载编排）
│   │   ├── SideloaderService.cs         #   爬虫 + 远程 ZIP 中央目录解析
│   │   ├── ConfigService.cs             #   统一配置（settings.json），改动发事件
│   │   ├── ScanCacheService.cs          #   扫描结果缓存（mods/useage，替代 IndexedDB）
│   │   └── GameLauncherService.cs       #   启动游戏/工作室、OpenInFolder、防休眠
│   ├── Models/                          # AppSettings、ModInfo、DownloadTask、ProxyInfo…
│   ├── ViewModels/                      # 每页一个 ViewModel（CommunityToolkit.Mvvm）
│   ├── Views/                           # 每页一个 Window（XAML）
│   │   ├── MainWindow.xaml              #   首页 + 导航（常驻）
│   │   ├── CharaWindow.xaml             #   角色卡浏览
│   │   ├── SceneWindow.xaml             #   场景库 + 智能整理
│   │   ├── CardExplorerWindow.xaml      #   单卡查看
│   │   ├── ModsWindow.xaml              #   本地模组
│   │   ├── SideloadWindow.xaml          #   BetterRepack 数据库
│   │   ├── DownloadWindow.xaml          #   下载任务
│   │   └── SettingsWindow.xaml          #   系统设置
│   ├── Controls/                        # CardGridControl、TaskItemControl…
│   ├── Windows/WindowManager.cs         # 窗口单例管理（Show/Hide/Activate）
│   └── Resources/sideload.zip           # EmbeddedResource
└── tests/HS2Tools.Tests/                # xUnit：二进制解析器回归测试
```

## 3. 技术映射（Go → C#）

| Go | C# |
|---|---|
| `net/http` + proxy + Range | `HttpClient` + `HttpClientHandler.Proxy`（含 `NetworkCredential` 认证）+ `RangeHeaderValue` |
| `archive/zip` | `System.IO.Compression.ZipArchive` |
| `compress/flate`（裸 DEFLATE） | `System.IO.Compression.DeflateStream`（**不能用 ZLibStream**） |
| `encoding/xml` | `System.Xml.Serialization` / `XDocument` |
| goquery（`table#indexlist tr` 选择器） | **HtmlAgilityPack** |
| `go:embed sideload.zip` | csproj `<EmbeddedResource>` + `Assembly.GetManifestResourceStream` |
| goroutine + WaitGroup + Mutex + 信号量 | `async/await` + `Task.WhenAll` + `SemaphoreSlim` + `Parallel.ForEachAsync` |
| `context.Context` 取消 | `CancellationTokenSource` |
| Wails EventsEmit/EventsOn | C# `event` / `IProgress<T>`（async 续体经 SynchronizationContext 自动回 UI 线程） |
| IndexedDB / localStorage / Go 配置 JSON（三处分散） | **统一一份** `%AppData%/hs2-tools/settings.json`（强类型 `AppSettings`），收藏、扫描缓存一并纳入 |
| F15 按键防休眠 hack | P/Invoke `SetThreadExecutionState(ES_CONTINUOUS \| ES_SYSTEM_REQUIRED)`，恢复用 `ES_CONTINUOUS` |
| `exec.Command`（启动游戏、explorer /select,） | `Process.Start`（`WorkingDirectory` 设为游戏目录） |
| React + Zustand | WPF XAML + `CommunityToolkit.Mvvm`（`ObservableObject`/`RelayCommand`） |

## 4. 多窗体架构

**原则：状态全部下沉到服务层单例，Window 只是视图。**

- **服务容器**：`App.xaml.cs` 启动时创建全部服务单例（ConfigService、ScanCacheService、DownloadManager、SideloaderService、GameLauncherService），以静态容器或构造注入提供。
- **WindowManager**：每个页面 Window 只创建一次；导航 = `Show()` + `Activate()`；用户点关闭 = 取消 Closing + `Hide()`——**保持状态**（滚动位置、筛选条件、已加载缩略图全部保留）。MainWindow 真正关闭时才 `Application.Current.Shutdown()`。
- **跨窗体通信 = 服务层事件**，窗口之间不直接引用：
  - 配置改动 → `ConfigService.Changed` 事件 → 各已打开窗口的 ViewModel 自行刷新；
  - 下载进度/完成 → `DownloadManager` 事件 → DownloadWindow 和 CharaWindow 的下载按钮各自订阅；
  - 爬虫进度/完成 → `SideloaderService` 事件。
- **订阅纪律**：ViewModel 在窗口 `Loaded`/`IsVisibleChanged` 时订阅，Hide 时退订，重开再订阅——防止重复订阅与泄漏。下载任务在 DownloadManager 中持续运行，**关闭 DownloadWindow 不中断下载**，重开时从任务表重建视图。

## 5. UI 页面映射

| 原 React 页面 | WPF 实现 |
|---|---|
| Home | MainWindow：功能卡片区（路径设置+校验、扫描+进度、**开始游戏/工作室（A1 修复）**、sideloader 更新、缺失 Mod 批量补全+进度、统计）+ 导航按钮 |
| CharaExplorer | CharaWindow：CardGridControl + **搜索框（A2 接通）** + 排序 ComboBox（~~筛选按钮 A3 砍~~）；详情用右侧/底部面板（大图、Mod 依赖列表+单项下载+**一键下载缺失（A4）**、名称/描述、**真实修改时间与大小（A6）**、~~标签（A5 砍）~~、打开所在文件夹） |
| Scenes | SceneWindow：场景库网格（同 Chara）+ 智能整理面板（创建任务/批量执行/进度，后台 `Task.Run` + `IProgress<int>`） |
| CardExplorer | CardExplorerWindow：`OpenFileDialog` + 人物/场景两种布局 |
| Mods / BetterRepack | `DataGrid`（内置排序/筛选/虚拟化），统计卡、搜索框（防抖）、下载按钮列 |
| Download | DownloadWindow：统计栏 + TabControl + 任务项列表（进度条/速度/剩余时间/取消/重试） |
| SystemSettings | SettingsWindow：代理表单（校验）+ 防休眠开关；改动即时生效并持久化 |

**关键控件——CardGridControl（卡片网格）**：

- `ItemsControl` + 虚拟化面板；WPF **无内置 VirtualizingWrapPanel**，需自带一个简单实现（约 200 行，0.5~1 天）或按行分组虚拟化。
- 数据流与原前端一致：全量路径 → 过滤/排序 → 可视区分批解析（C# 版**一次读盘**同时拿名称+缩略图，修掉原版读两遍的问题），缩略图 `BitmapImage` 做 LRU 缓存控内存。
- 收藏存统一配置文件；排序用 `StringComparer.Create(new CultureInfo("zh-CN"), …)` 对齐原版中文排序行为。

**主题（A7）**：单主题。`ResourceDictionary` 集中定义颜色/画刷资源，后期加第二主题时换字典即可，控件一律用 `DynamicResource` 引用，不留硬编码色值。

## 6. 异步与线程模型

- 所有 IO/网络/解析走 `async/await`；CPU 密集的批量解析用 `Parallel.ForEachAsync`（并发度：文本 8 / 图像 4 / zipmod 4 / 爬虫 mod 10、目录 3，照搬原版）。
- 进度上报 `IProgress<T>` 或服务事件（内部注意封送到 UI 线程：`IProgress` 天然安全；自建 event 由订阅方用 Dispatcher 处理）。
- 取消一律 `CancellationTokenSource`：DownloaderService 的活动任务表（对应原 `activeDownloads` map）、SideloaderService 单实例运行锁（对应 `sideloaderMu`）。
- 长任务（扫描、爬虫、整理）全部后台执行，UI 只订阅进度。

## 7. 存储设计（统一配置）

`%AppData%/hs2-tools/settings.json`，强类型：

```json
{
  "gamePath": "…",
  "proxy": { "uri": "http://127.0.0.1:7890", "username": "", "password": "" },
  "preventSleep": false,
  "favorites": ["…卡片完整路径…"],
  "localMods": { "guid": { "name": "…", "version": "…", "path": "…" } },
  "modUsage": { "guid": 3 }
}
```

- `ConfigService` 负责读写（改动即发事件、防抖落盘）；派生路径 `UserData/chara/female`、`UserData/Studio/scene`、`mods` 由它提供。
- 相对路径常量与 exe 名照搬原版：`HoneySelect2.exe`、`StudioNEOV2.exe`、`UserData/Studio/scene`、`UserData/chara/female`、`mods/hs2-tool-download`。
- 统一配置后，**A1 启动游戏自然修复**（原版双轨制根因消除）。

## 8. 功能范围最终清单

**保留（含修复项）**：路径设置与校验；数据分析扫描；启动游戏/工作室（A1 修复）；sideloader 更新（运行/停止/日志）；缺失 Mod 批量补全；角色卡/场景卡网格（无限滚动、排序、收藏、搜索 A2）；详情面板（大图、Mod 依赖、单项下载、一键下载缺失 A4、真实修改时间/大小 A6、打开所在文件夹）；场景智能整理；单卡查看器；本地模组表格；sideload 数据库浏览与下载；下载任务管理（取消/重试/清空/统计，不持久化）；代理设置；防休眠。

**砍掉**：筛选按钮占位（A3）、详情标签（A5）、双主题（A7 暂缓留口）、14 个死 API、7 个死组件、Go 端配置 JSON、IndexedDB 废 store、双套下载状态、独立 CLI、F15 hack、跨平台分支、批量 API 分批壳。

## 9. 移植注意点（实现期对照）

逐条对应 [01-现状分析.md](01-现状分析.md) 第 7 节的复刻清单。额外提醒：

- Go `string(bytes)` 对无效 UTF-8 保留原始字节，.NET `Encoding.UTF8.GetString` 默认替换为 U+FFFD——乱码卡名输出可能不同，测试期用真实卡片对比确认可接受。
- 所有原版"静默吞错"点位（批量解析失败跳过、爬虫单页失败直接返回）迁移时**至少记日志**，不要默默 continue。
- `SideloaderService.Run` 设计为可重入（每次运行新建内部状态），修掉原版非可重入的坑。

## 10. 实施期修订记录（阶段 1 实测发现，2026-07）

阶段 1 移植过程中发现并处理的 .NET 平台差异与对原方案的偏离。后续阶段实现网络/IO 功能前先读本节。

### 10.1 .NET HttpClient 的三个平台陷阱（已修复，均有测试）

| 陷阱 | 现象 | 对策（已落实） |
|---|---|---|
| **默认走系统代理** | `HttpClientHandler` 未设 Proxy 时使用系统代理（Go 自建 Transport 未配代理时直连），导致爬虫绕路代理、行为与 Go 不一致 | 未配置代理时显式 `handler.UseProxy = false`（SideloaderService / DownloaderService） |
| **HTTP/2 设置无效** | `HttpClient.DefaultRequestVersion = 2.0` **不生效**（请求消息 `Version` 默认 1.1 覆盖客户端默认）；目标站点（Apache）对 h1 回 `Connection: close`，每请求新建 TCP+TLS → 速度仅 Go 的 1/3、连接 churn 致进程崩溃 | 在**每个** `HttpRequestMessage` 上设 `Version = 2.0, VersionPolicy = RequestVersionOrLower`（修复后 300+ mod/分钟、连接数 2 条、内存 <40MB，与 Go 相当） |
| **Range 被 200 应答** | 代理干预时 Range 请求可能收到 200 全量响应；若按 Go 原样 `ReadAll` 并前插累积，16 次 × 全文件 × 10 并发 → 数 GB 内存 | 200 且非整文件请求的块**丢弃**（Range 正常站点恒为 206，此分支仅代理干预时触发）；≤64KB 小文件整取后本地头/manifest **内存切片**不再发请求 |

### 10.2 安全防御（真实站点未触发，纯兜底）

- `SideloaderService`：manifest 压缩大小 > 4MB 视为解析异常，跳过并记日志。
- `ZipRemoteReader.ExtractManifestGuid`：DEFLATE 解压上限 16MB，防解压炸弹。

### 10.3 与 Go 版的行为差异说明（非缺陷，已确认）

- **下载"服务器不支持续传，重新开始"提示**：Go 源码该分支不可达（200 时 `startPos` 已先重置为 0），C# 版同样保留为不可达代码；实际整文件重写行为一致。
- **爬虫重复 guid 的相对路径**：站点上同一 guid 存在于多个目录（如 Exclusive HS2/AIS），结果 dict"后写覆盖"；Go 版因 map 随机序选路本身就是随机的，C# 版可能选出不同目录——对照验证按重合度 + 差异占比（≤1%）判定，不要求路径逐一致。
- **DownloadManager 同名任务**：原版直接覆盖 `activeDownloads` 条目（旧任务失控），C# 版拒绝重复触发（返回 false）。
- **测试基础设施**：读取 Go CLI 进程输出须显式 `StandardOutputEncoding = UTF8`（默认 ANSI/GBK 码页会破坏 JSON）；Go sideloader CLI 需要 go ≥ 1.25 工具链（本机 1.24 + 自动下载 1.25.12 可用）。
- 阶段 0 决策：测试夹具不收集真实卡片/zipmod 大文件，改为**程序化合成夹具**（PNG 标记字节、Store/Deflate/脏 manifest zipmod、多 dummy 条目大 zipmod、autoindex HTML），可重复且无仓库膨胀；真实 sideload.zip 照常内嵌。

### 10.4 阶段 3（页面迁移）实施期修订与原版 bug 修复（2026-07）

**修复的原版 bug（功能从未生效，C# 版按意图实现）：**

| 位置 | 原版问题 | C# 版对策 |
|---|---|---|
| sideloader 更新 | 爬虫结果只经 `sideloader:complete` 事件发出且前端丢弃，前端随后 `init()` 重读的是**内嵌旧库**——"更新"从不生效 | 新增 `SideloadDatabaseService`：结果落盘 `%AppData%/hs2-tools/sideload.json`；加载顺序：用户库 > 内嵌库 |
| 场景智能整理 | `moveFile(scenePath, targetFolder)` 把**目录**当目标路径；`os.Rename(文件→已存在目录)` 恒失败，错误只进 console——移动从未发生 | 目标补全文件名：`hs_tools_<文件夹>/<场景文件名>` |
| 停止爬虫状态 | 前端订阅的 `sideloader:stopped` 事件 Go 端从不 emit；取消后走 complete{success:true} 显示"更新成功" | VM 侧 `_stopRequested` 标志：取消后完成不更新数据库、状态显示"已停止" |

**实现口径决策：**

- **代理校验放行 socks5**（有意超集）：原版 `validateProxyUrl` 只放行 http/https，但 `getProxyString` 本就支持 socks5 认证拼接，按方案表格口径放行。
- **下载页"失败"Tab 只含 Failed 不含 Cancelled**（照原版 `getFailedTasks` 只筛 `error`）；已取消任务只在"全部"Tab 出现。
- **"清除完成"清所有非下载中任务**：`DownloadManager.ClearFinished` 是阶段 1 既有语义（清 Completed/Failed/Cancelled），与原版 `clearCompleted`（仅清 completed+cancelled）略宽；按钮可见性已按新语义自洽并注释。
- **sideload"已存在"判定大小写敏感**（保真）：原版 `mods[guid]` 精确匹配，C# 照做；ModsWindow 的使用次数匹配才是大小写不敏感（照原版 createUsageMap 双向小写化）。
- **zh-CN 排序用 ICU 行为**：.NET 8 `StringComparer.Create(zh-CN)` 与浏览器 `localeCompare(zh-CN)` 同为 ICU，汉字按拼音排在拉丁字母前——与原版一致。
- **详情面板为内嵌底部面板**（原版是 70% 高 Drawer 抽屉）；角色/场景共用 `CardDetailPanel`。
- **场景整理进度分析用批量 API**（`ReadPngNamesBatchAsync` 每批 24 并行），替代原版逐文件 IPC 循环；进度语义 0~50 分析 / 50~100 移动不变。
- **VM 订阅纪律**：窗口与 VM 均为单例与服务同寿，永久订阅服务事件（方案 §4 的"Hide 退订"针对非常驻对象，此处不适用）。

### 10.5 阶段 4（打磨打包）实施期修订（2026-07）

**修复的 C# 版自身缺陷（三审计发现）：**

| 位置 | 问题 | 对策 |
|---|---|---|
| CardGridControl.xaml | 缩略图 Image 可见性触发器写反（默认 Collapsed、null 时 Visible）——缩略图从不显示、"暂无预览"常驻（阶段 3 冒烟不渲染缩略图故漏网） | 可见性逻辑移到占位 TextBlock（参照 CardExplorerWindow 正确写法） |
| ConfigService | 防抖写盘失败在线程池 Timer 回调抛出 → **进程崩溃**（DispatcherUnhandledException 管不到），设置页却显示"已保存" | WriteLocked try/catch：记 ErrorLog、保持脏标记下次重试、永不抛出 |
| CardGridControl | reset（刷新/搜索/排序）撞上在飞批次时新加载被 `_isLoadingMore` 挡掉且无人接力 → 网格停空态 | finally 复位后若批次被取消且仍有未加载项则接力触发 |
| DownloadManager/下载页 | 终态同名任务重新 StartDownload 重发 TaskAdded → 列表同 Id 双行、旧行僵尸 | OnTaskAdded 按 Id 去重 |
| CardDetailViewModel | LoadCoreAsync 无 try/catch，文件竞态下任务 faulted 无人观察 | try/catch 记 ErrorLog |

**口径决策：**

- **ErrorLog 统一设施**（`Services/ErrorLog.cs`）：原版"静默吞错"点位（§9 要求"至少记日志"）全部落实——配置写盘/损坏回退、下载失败、爬虫单页与单 mod 失败链、数据库损坏回退、目录扫描跳过、非法代理回退、单卡解析失败。测试经 `TestAssets` 静态构造把 ErrorLog 重定向到临时目录，不污染真实日志。
- **扫描 ∥ 补全 ∥ 爬虫 ∥ Mods 刷新允许并行**（与原版一致，审计 G4 确认）：数据各写各的（Config.Update 有锁），唯一交叉是补全写入 mods 目录与扫描阶段 1 并发——在途文件解析失败走 onError 记日志跳过，行为自洽。
- **补全结束主页显示汇总**（"成功 X，失败 Y"）：原版失败只在下载页可见，主页进度照常走完。
- **"所有 Mods 已就绪"门控 HasScanData**（ModCount>0 或 UsageCount>0）：无扫描数据时不再误显示。
- **整理执行中删除/清空按钮置灰**（CanExecute）：阶段 3 是"功能上拦截但按钮可点、点击静默无效"。
- **乱码卡名差异终验通过**（§9 挂起项）：真实 15,141 卡（含 5 张乱码名卡）与 Go CLI 输出完全一致——Go `encoding/json` 输出边界同样做 U+FFFD 替换，.NET 解码替换差异在应用边界不可见。
- **审计误报记录**：CharaWindow/SceneWindow 搜索"未实时过滤"不成立——`SearchText="{Binding Text, ElementName=SearchBox}"` 以 TextBox.Text 为源即逐键传播（UpdateSourceTrigger 只管反向）；Chara/Scene `LoadCardPaths` UI 线程同步扫目录为已知观察项（大目录轻微卡顿，真实 935 场景目录实测无感）。
